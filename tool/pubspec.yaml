name: tool
description: A sample command-line application.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.2.4 <4.0.0'

dependencies:
  # For parsing the command line args
  args: 2.4.2
  collection: 1.18.0
  # for parsing the ios info.plist file and check out the supported languages
  xml: 6.5.0

dev_dependencies:
  flutter_lints: 3.0.1